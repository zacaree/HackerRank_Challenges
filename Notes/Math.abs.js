// Snippet below was taken from challenge: Bon Appetit.
const [a, b] = [10, 6];

// Math.abs() is great because either way, b - annaOwes or annaOwes - b,
// it returns the same positive number
console.log(Math.abs(a - b));
console.log(Math.abs(b - a));
