// Work in progress!!!

// [n, k] = [[5, 8, 2], [2, 5, 2]];
const arr = ["5 2", "8 5", "2 2"];
[n, k] = arr.map(set => set.split(" "));

function getMaxLessThanK(n, k) {
  console.log(n, k);
}

getMaxLessThanK(n, k);
